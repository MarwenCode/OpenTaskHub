version: "3.9"

services:
  db:
    image: postgres:16             # dernière version stable
    container_name: opentaskhub-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: marwen
      POSTGRES_DB: opentaskhub
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
