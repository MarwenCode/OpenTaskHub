# Example environment variables for OpenTaskHub backend
# Copy this file to `.env` and fill real values. Do NOT commit your real `.env` to source control.

# PostgreSQL configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=opentaskhub
DB_USER=postgres
DB_PASSWORD=marwen

# JWT secret used to sign tokens


# Server configuration
PORT=3000
NODE_ENV=development
