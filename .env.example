# Copy this file to ".env" at the repository root and fill real values.
# This file is safe to commit because it contains placeholders only.

# Backend app settings
PORT=5000
NODE_ENV=development
JWT_SECRET=change_me_jwt_secret
ADMIN_SECRET=change_me_admin_secret

# Database connection used by backend container
DB_HOST=db
DB_PORT=5432
DB_NAME=opentaskhub
DB_USER=postgres

# Postgres container initialization
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_db_password
POSTGRES_DB=opentaskhub
